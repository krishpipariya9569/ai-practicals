# D = Dirty, C = Clean
room = ["D","C","D","D","C"]
def spot_room(room):
  picture = ""
  for spot in room:
    if spot == "D":
      picture += "❌"
    else:
      picture += "✅"
  print(picture)
print("Our room:")
spot_room(room)


def clean_spot(spot):
  if spot == "D":
    return "C"
  else:
    return "C"

  result = clean_spot("D")
  print("The robot looked at dirty spot and made it")


  spot_room(room)
print()
print("before cleaning:")


for i in range(len(room)):
 room[i] = clean_spot(room[i])
 print("after cleaning spot number", i+1,":")
 spot_room(room)

 print()
 print("after")


 
